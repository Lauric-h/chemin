<form method="post" action="{{ path('training_plan_delete', {'id': training_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_plan.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
