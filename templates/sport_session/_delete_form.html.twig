<form method="post" action="{{ path('sport_session_delete', {'id': sport_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sport_session.id) }}">
    <button class="btn">Delete</button>
</form>
